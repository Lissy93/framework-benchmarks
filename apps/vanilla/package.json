{
  "name": "weather-app-vanilla",
  "version": "1.0.0",
  "description": "Weather app built with vanilla JavaScript",
  "main": "index.html",
  "scripts": {
    "dev": "python3 -m http.server 3000 || python -m http.server 3000",
    "build": "echo 'No build step required for vanilla JS'",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "keywords": ["weather", "vanilla-js", "frontend"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}